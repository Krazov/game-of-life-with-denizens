define(Object.freeze({
    STATUS_CHANGED: 'statusChanged',

    TICK_LENGTH: 1000,

    // TODO: move to createBoard
    neighboursGrid: [[-1, -1], [0, -1], [1, -1], [-1, 0], /* x */ [1, 0], [-1, 1], [0, 1], [1, 1]]
}));